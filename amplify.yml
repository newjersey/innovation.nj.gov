version: 1
frontend:
  phases:
    build:
      commands:
        - bundle install
        - npm run build
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: _site
    files:
      - "**/*"
  cache:
    paths: []
