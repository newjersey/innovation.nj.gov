{
  "dependencies": {
    "@newjersey/njwds": "^1.0.0",
    "clean-css": "^5.1.3",
    "uncss": "^0.17.3"
  },
  "scripts": {
    "build": "bundle exec jekyll build && node ./build.js && bundle exec jekyll build"
  }
}
